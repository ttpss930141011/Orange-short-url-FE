// ? 全局不動配置項 只做導出不做修改

// * 首頁地址（默認）
export const HOME_URL: string = '/home/index'

// * Tabs（黑名單地址，不需要添加到 tabs 的路由地址，暫時沒用）
export const TABS_BLACK_LIST: string[] = ['/403', '/404', '/500', '/layout', '/login', '/dataScreen']

// * 高德地圖key
export const MAP_KEY: string = ''
